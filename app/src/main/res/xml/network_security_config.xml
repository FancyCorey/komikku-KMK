<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release and preview artifacts use encrypted transport and system CAs by default. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
